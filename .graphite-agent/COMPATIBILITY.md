# V7.1 Compatibility Guide

Use `analyse.py --legacy-analyser <path>` to run local V6.4 variants first. V7.1 reads common cached artefacts: `.graphite-agent/analysis_snapshot.json` and `.graphite-agent/plan.json`. Unknown local enhancement fields are preserved.
