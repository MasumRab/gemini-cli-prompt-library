{
  "name": "gemini-cli-dspy-integration",
  "version": "1.0.0",
  "description": "DSPy Integration Adapter for Render Node.js Runtime (DO NOT DELETE - required for deployment fallback)",
  "scripts": {
    "postinstall": "command -v pip >/dev/null 2>&1 && pip install -r requirements.txt || echo 'pip not available, skipping Python dependency installation'",
    "start": "python -m dspy_integration.cli"
  },
  "dependencies": {},
  "devDependencies": {}
}
